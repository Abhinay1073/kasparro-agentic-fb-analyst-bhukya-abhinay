# Facebook Ads Performance Analysis Tool

A simple R tool to analyze Facebook Ads performance and improve ROAS (Return on Ad Spend) and CTR (Click-Through Rate).

## 🚀 Quick Start

### 1. Download & Setup
```r
# Install required packages
install.packages(c("dplyr", "tidyr", "lubridate", "stringr", "jsonlite", "zoo"))

# Run the analysis
source("fb_ads_analysis.R")
run_full_analysis()
